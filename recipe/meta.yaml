{% set version = "2.6.0.4" %}

{% set cuda_major = cuda_compiler_version.split(".")[0]|int %}

{% set platform = "linux-x86_64" %}  # [linux64]
{% set platform = "linux-sbsa" %}  # [aarch64]
{% set platform = "windows-x86_64" %}  # [win]
{% set extension = "tar.xz" %}  # [not win]
{% set extension = "zip" %}  # [win]

{% set sha = "28dbda315a95b3edc58af986ff1b886dcf2bd1c9651094539011bbce234cb9ae" %}  # [linux64 and (cuda_compiler_version or "").startswith("12")]
{% set sha = "f057367cabfc459b5e66e295b724b00ed780defda3ccfb62a98c8817df46b8b5" %}  # [aarch64 and (cuda_compiler_version or "").startswith("12")]
{% set sha = "05e29d4dd2472b2261893979f2e35b7ef27563ba47976ecdc121d88660d86186" %}  # [win64 and (cuda_compiler_version or "").startswith("12")]
{% set sha = "f70003a4817da3db2f1f7726328c31332ada89a496446fe30b90f414eccda487" %}  # [linux64 and (cuda_compiler_version or "").startswith("13")]
{% set sha = "f9e85439174f64d6e84db9ea54b101a24ca751b7e94ceccbfd5181be22980310" %}  # [aarch64 and (cuda_compiler_version or "").startswith("13")]
{% set sha = "005257fb07789669605eaa4e6e5cf46a55dbade3194db03931cdbb900711e626" %}  # [win64 and (cuda_compiler_version or "").startswith("13")]

package:
  name: cutensor
  version: {{ version }}

source:
  - url: https://developer.download.nvidia.com/compute/cutensor/redist/libcutensor/{{ platform }}/libcutensor-{{ platform }}-{{ version }}_cuda{{ cuda_major }}-archive.{{ extension }}
    sha256: {{ sha|default("") }}
  # Delegate git-clone from test scripts to the recipe
  - git_url: https://github.com/NVIDIA/CUDALibrarySamples.git
    # Update the commit hash on version bump
    git_rev: 241afd3c7a8d1d756100139a3f629df5475fcf5c
    folder: sample_linux

build:
  number: 0
  skip: true  # [osx or cuda_compiler_version in (undefined, "None")]
  run_exports:
    - {{ pin_subpackage('cutensor') }}
  ignore_run_exports_from:
    - {{ compiler('cuda') }}
  missing_dso_whitelist:
    - "*/libcuda.so.*"    # [linux]
    - "*/nvcuda.dll"      # [win]
  script_env:
    - cuda_major={{ cuda_major }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
    - {{ compiler('cuda') }}
    - arm-variant * {{ arm_variant_type }}  # [aarch64]
    - cf-nvidia-tools 1                     # [linux]
    - msys2-git                             # [win]
    - git                                   # [not win]
  host:
    - cuda-version {{ cuda_compiler_version }}
    - libcublas {{ libcublas }}
  run:
    - {{ pin_compatible("cuda-version", min_pin="x", max_pin="x") }}
    - libcublas {{ libcublas }}
  run_constrained:
    - arm-variant * {{ arm_variant_type }}  # [aarch64]

test:
  source_files:
    - sample_linux/
  requires:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('cuda') }}
    - {{ stdlib('c') }}
    # need the libcuda stub for import test
    - cuda-driver-dev  # [linux]
  files:
    - test_load_elf.c  # [linux]

about:
  home: https://developer.nvidia.com/cutensor
  license: LicenseRef-cuTENSOR-Software-License-Agreement
  license_url: https://docs.nvidia.com/cuda/cutensor/license.html
  license_file: LICENSE
  license_family: Other
  summary: Tensor Linear Algebra on NVIDIA GPUs
  description: |
    The cuTENSOR Library is a first-of-its-kind GPU-accelerated tensor linear
    algebra library providing tensor contraction, reduction and elementwise
    operations. cuTENSOR is used to accelerate applications in the areas of
    deep learning training and inference, computer vision, quantum chemistry
    and computational physics.
  doc_url: https://docs.nvidia.com/cuda/cutensor/index.html
  dev_url: https://developer.nvidia.com/cutensor/downloads

extra:
  skip-lints:
    - deprecate_license_url
  recipe-maintainers:
    - ChrisPsa
    - b-kloss
    - conda-forge/cuda
    - v0i0
    - yangcal
    - JeremyWangNVDA
    - leofang
    - jakirkham
    - mtjrider
